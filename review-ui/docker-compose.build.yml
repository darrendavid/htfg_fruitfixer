# Local build override — use when you need to build the image from source:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up --build
services:
  review-ui:
    image: ghcr.io/darrendavid/htfg_fruitfixer/review-ui:latest
    build:
      context: .
